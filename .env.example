# Only used by docker-compose.yaml to configure the local Postgres container.
# The app services (backend/frontend) are NOT started by docker-compose and
# read their own env from backend/.env and frontend/.env.local — see
# backend/.env.example and frontend/.env.example for those.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root
POSTGRES_DB=skillswap
