NEXT_PUBLIC_API_URL=http://localhost:5200/api
# Optional. Public origin used for canonical/OpenGraph URLs and the sitemap.
# On Vercel this is derived automatically from VERCEL_PROJECT_PRODUCTION_URL,
# so only set this to override it (e.g. a custom domain).
# NEXT_PUBLIC_SITE_URL=https://skill-swap-ai.vercel.app
